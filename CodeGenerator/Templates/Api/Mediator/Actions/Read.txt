using HelpersForCore;
using MediatR;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using {{ProjectName}}.Data.Models;

namespace {{ProjectName}}.Controllers.{{Plural}}.Actions
{
    public class Get{{Singular}}
    {
        {{Request}}
        
        public class Response : SingleResponse<{{Singular}}>
        {
            public Response({{Singular}} result) : base(result)
            {
            }
        }
        
        public class Handler : IRequestHandler<Request, Response>
        {
            private readonly DatabaseContext databaseContext;

            public Handler(DatabaseContext databaseContext)
            {
                this.databaseContext = databaseContext;
            }
			
            public async Task<Response> Handle(Request request, CancellationToken token)
            {
                {{Singular}} {{singular}} = await databaseContext.{{Plural}}
                    .FirstOrDefaultAsync(x => x.{{}} == request.{{}});
                return new Response({{singular}});
            }
        }
    }
}
