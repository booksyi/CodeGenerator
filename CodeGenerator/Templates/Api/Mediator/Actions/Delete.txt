using HelpersForCore;
using MediatR;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using {{ProjectName}}.Data.Models;

namespace {{ProjectName}}.Controllers.{{Plural}}.Actions
{
    public class Delete{{Singular}}
    {
        {{Request}}
        
        public class Handler : IRequestHandler<Request, SingleResponse<bool>>
        {
            public Handler()
            {
            }
			
            public async Task<SingleResponse<bool>> Handle(Request request, CancellationToken token)
            {
                // TODO: Delete Data
                return new SingleResponse<bool>(true);
            }
        }
    }
}
